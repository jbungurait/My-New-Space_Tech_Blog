<!-- Loop over each object in the projects array -->

{{#each blogposts as |blogpost| }}
<div class="row mb-4 blogpost">
  <div class="col-md-5">
    <h2>
      <a href="/blogpost/{{blogpost.id}}">{{blogpost.title}}</a>
    </h2>
    <p>Created by {{blogpost.user.userName}} on {{blogpost.date}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{blogpost.post}}
    </p>
  </div>
</div>
{{/each}}
